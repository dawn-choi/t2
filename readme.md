# Reptile Management App

This repository contains a simple command line application for managing reptiles.

## Running the app

Run the following command to start the program:

```bash
python reptile_app.py
```

The app stores reptile data in `reptiles.json` in the same directory.
